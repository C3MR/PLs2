-- سيتم تحديث هذا بعد إنشاء المستخدم في Auth
-- يمكنك الحصول على user_id من صفحة المستخدمين في Supabase وتشغيل هذا الكود:

-- UPDATE public.profiles 
-- SET user_id = 'USER_ID_FROM_AUTH' 
-- WHERE email = 'c.3mr@hotmail.com';

-- في الوقت الحالي، سأضع تعليق لتذكيرك
SELECT 'تم إعداد قاعدة البيانات - يرجى إنشاء المستخدم في Supabase Auth أولاً' as status;